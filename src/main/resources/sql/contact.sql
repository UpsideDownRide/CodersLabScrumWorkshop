CREATE TABLE contact (
                      id INT AUTO_INCREMENT,
                      name VARCHAR(50),
                      email varchar(50),
                      address varchar(50),
                      phone int,
                      description varchar(200),

                          PRIMARY KEY(id)
);
